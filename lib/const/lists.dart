import 'package:emart_seller/const/const.dart';

const profileIconList = [icShopSettings, icChat, icAccount];
const profileIconTitle = [settings, messages, logout];

const popupMenutitle = [featured, remove];
const popupMenuIconsList = [Icons.featured_play_list, Icons.delete];

const profileButtonTitles = [shopSettings, messages, editProfile, showUsers];
const profileButtonIcons = [
  Icons.settings,
  Icons.chat,
  Icons.edit,
  Icons.person_search
];
